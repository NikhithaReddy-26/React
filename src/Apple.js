import React from 'react';

class Apple extends React.Component{
    render()
    {
        return(
            <div><center><h2>Name={this.props.name} Age={this.props.age} From={this.props.} </h2></center></div>
        )
    }
}
export default Apple;